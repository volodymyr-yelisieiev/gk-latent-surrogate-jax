# Data Directory

Raw datasets and generated local fixtures are intentionally not tracked by git.

Use `gks make-synthetic-h5 --config configs/data/tiny_dummy.yaml --output-dir outputs/synthetic_h5`
to generate a small HDF5 fixture for local inspection and tests.
