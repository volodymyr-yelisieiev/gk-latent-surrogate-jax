\cleardoubleoddpage
\begin{otherlanguage}{english}
\begin{abstract*}
Nonlinear gyrokinetic simulations evolve high-dimensional distribution functions and are
computationally demanding. This thesis investigates whether a compact latent surrogate can predict
short-horizon dynamics while retaining information needed for turbulent-transport diagnostics. A
GPU-first pipeline is implemented in JAX and Flax for channel-first five-dimensional snapshots. A
convolutional encoder maps each snapshot to a 128-dimensional latent vector and is trained with a
SimSiam-style consistency objective plus supervised heads for scalar flux and two one-dimensional
spectra. Persistence, gated recurrent unit, and causal Transformer models then predict future
latents from an eight-step context.

A data-integrity audit found that an earlier comparison used inconsistent trajectory-split seeds
between representation learning and downstream evaluation; those numbers were withdrawn. The
corrected protocol fixes seed 52 throughout, selects the learned model on five validation
trajectories, and evaluates it once on five untouched test trajectories. The cache-normalized
Transformer was the best learned validation candidate. On the locked eight-step test, its flux root
mean squared error is 11.4255, compared with 11.9889 for persistence, a 4.70\% reduction in the
primary point estimate. It also reduces latent mean squared error and flux mean absolute error.
Per-trajectory flux RMSE improves in three of five cases, while a descriptive paired 95\% interval
includes zero; the small sample therefore does not support a significance claim. The work
weights rollout aggregates equally by trajectory, and every accepted artifact is bound to an
auditable split manifest. It
therefore contributes a reproducible, leakage-resistant latent-surrogate pipeline and bounded
evidence of short-horizon improvement; it does not establish full-field reconstruction, long-horizon accuracy, or a
direct numerical advantage over GyroSwin.
\end{abstract*}
\end{otherlanguage}

\cleardoubleoddpage
\begin{otherlanguage}{ngerman}
\begin{abstract*}
Nichtlineare gyrokinetische Simulationen entwickeln hochdimensionale Verteilungsfunktionen und sind
rechnerisch aufwendig. Diese Arbeit untersucht, ob ein kompaktes latentes Ersatzmodell kurzfristige
Dynamik vorhersagen und zugleich Informationen für Diagnosen des turbulenten Transports erhalten
kann. Dazu wird eine GPU-orientierte Pipeline in JAX und Flax für kanalorientierte
fünfdimensionale Momentaufnahmen implementiert. Ein konvolutioneller Encoder bildet jede
Momentaufnahme auf einen 128-dimensionalen latenten Vektor ab und wird mit einem
SimSiam-Konsistenzziel sowie überwachten Köpfen für skalaren Fluss und zwei eindimensionale Spektren
trainiert. Persistenz-, GRU- und kausale Transformer-Modelle sagen anschließend aus acht
Kontextschritten zukünftige latente Zustände voraus.

Eine Datenintegritätsprüfung zeigte, dass ein früherer Vergleich unterschiedliche Seeds für die
Trajektorienaufteilung verwendete; diese Zahlen wurden zurückgezogen. Das korrigierte Protokoll
verwendet durchgehend Seed 52, wählt das gelernte Modell auf fünf Validierungstrajektorien und
wertet es einmalig auf fünf unberührten Testtrajektorien aus. Der cache-normalisierte Transformer
war der beste gelernte Validierungskandidat und erreicht im gesperrten Acht-Schritt-Test einen
Fluss-RMSE von 11,4255 gegenüber 11,9889 für Persistenz. Dies entspricht einer Verringerung des
primären Punktschätzers um 4,70\,\%. Der Fluss-RMSE verbessert sich in drei von fünf einzelnen
Testtrajektorien; ein deskriptives gepaartes 95\,\%-Intervall schließt jedoch null ein, weshalb keine
Signifikanz behauptet wird. Der Beitrag umfasst eine nach Trajektorien gleich gewichtete Auswertung,
eine überprüfbare Zuordnung jedes akzeptierten Artefakts zur Datenaufteilung sowie eine
reproduzierbare, gegen Datenleckage abgesicherte Pipeline mit klar begrenzter empirischer Evidenz;
vollständige Feldrekonstruktion, Langzeitgenauigkeit und ein direkter numerischer Vorteil
gegenüber GyroSwin werden nicht behauptet.
\end{abstract*}
\end{otherlanguage}
